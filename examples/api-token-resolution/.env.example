# Environment variables for API-based token resolution example

# JWT secret (shared with tenant manager)
JWT_SECRET=your-shared-jwt-secret-min-32-chars

# Tenant manager API URL
TENANT_MANAGER_URL=https://tenant-manager.example.com

# Service token for MCP server → tenant manager authentication
SERVICE_TOKEN=service-token-for-api-calls
